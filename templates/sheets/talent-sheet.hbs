<form class="item-sheet" autocomplete="off">
    <header class="sheet-header flexrow">
        <img class="item-img" src="{{item.img}}" data-edit="img" title="{{item.name}}" />
        <div class="header-details flexcol">
            <h1 class="item-name">
                <input name="name" type="text" value="{{item.name}}" placeholder="Item Name" />
            </h1>
            <div class="item-type">Talent</div>
        </div>
    </header>

    <section class="sheet-body">
        <!-- Attack With -->
        <div class="form-group">
            <label for="system.prerequisite">Prerequisite</label>
            <input type="text" name="system.attackWith" value="{{item.system.prerequisite}}" />
        </div>

        <!-- Description -->
        <div class="form-group description-field">
            <label>Description</label>
            <textarea name="system.description">{{item.system.description}}</textarea>
        </div>
        
    </section>
</form>
